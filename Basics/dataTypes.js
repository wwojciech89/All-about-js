
// Data Types

// In JS we have dynamic and weakly typing

// Dynamic typing is when you don't need to set type of value, value define type of data.

// Weak typing is when veriable can change its own type of value during code execution.


// STRING
const text = "hello"; // typeof text = string

//  we can use " ", ' ', ` `

// NUMBER

const number = 1; // typeof number = number

// also number are:

// NaN - e.g. 4 / house will give us NaN
// Infinity , -Inifinity

// BOOLEAN

const checked = true;  // typeof checked = boolean
const used = false;  // typeof used = boolean

// UNDEFINED

let word;  // typeof word = undefined

// NULL

const noValue = null;  // typeof noValue = object; its not null becouse of needs of backward compatyblility

// SYMBOL

const symbol = Symbol('use'); //  typeof symbol = symbol

// OBJECTS

 const obj = {                 // typeof obj = object 
     key: value,
     string: "hello",
     number: 1
 }


 // Falsy Values:

 // values that are treated as false when we use them in boolean context

 // false
 // null
 // udefined
 // NaN
 // ""
 // 0
 // -0
 // 0n






